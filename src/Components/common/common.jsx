import React from 'react';
import { Header, Navbar } from './index.js';

const common = () => {
    return (
        <div>
            <Header />
            <Navbar />
        </div>
    );
};

export default common
